``<widget-model-disable-nbrecords-computation>`` is a backend widget that makes possible to disable the computation of nbRecords on domain fields.
See `source repository <https://github.com/oca/web>`_ 
