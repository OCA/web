Adds interactive row merging to Odoo's Pivot view. Hover over a row header
to reveal a "+" button that fuses that row with the one below into a single
combined row with summed measurements and a composite title (e.g. "B + C").
A "×" button on the merged row splits it back. State is session-only and
resets on navigation or reload.
