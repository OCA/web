This module provides a mixin that can be inherited by any model
to add popup buttons to x2many list fields in form views.

**Features:**

- Adds a popup button to editable lists in form views
- Opens records in a form dialog instead of inline editing
- Works with both existing and new (NewID) records
- Optional activation - no hard dependencies required
