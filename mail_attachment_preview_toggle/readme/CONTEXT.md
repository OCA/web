Users who regularly work with documents attached to records may want to reclaim 
horizontal space on large screens when the attachment preview is not needed.

This module adds a personal preference and contextual form buttons so each user can
decide whether the attachment preview panel should be displayed.

The module targets the standard mail attachment preview used by forms containing
`div.o_attachment_preview`. It does not change attachment storage, chatter behavior,
or report generation.
