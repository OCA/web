In developer mode, column tooltips inside a **One2many** list show the parent
technical field name (for example `bom_line_ids`) right under **Field:**.

Many2many fields are intentionally left alone: they already expose the field
name via the FormLabel `?` tooltip.
