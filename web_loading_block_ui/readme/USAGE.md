Trigger any action that takes longer than the configured delay, for example
opening a heavy report or saving a large record. The screen is blurred and a
centered "Loading..." spinner is shown until the action finishes. Requests
marked as silent by Odoo (background polling, notifications) never block the
screen.
