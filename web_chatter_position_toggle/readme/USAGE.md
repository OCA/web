After installing the module, a toggle button (↔) will appear in the control
panel of form views that have a chatter. Click the button to switch the chatter
between sided and bottom positions.

The toggle is per-form-view and resets when navigating away.
